import chai from 'chai';
const expect = chai.expect;

describe('See if the tests are running', function() {
  this.beforeEach();

  it('', function() {
    expect().to.equal();
  });

  it('', function() {
    expect().to.equal();
  });

  it('', function() {
    expect().to.equal();
  });

  it('', function() {
    expect().to.equal();
  });

  it('', function() {
    expect().to.equal();
  });
});
