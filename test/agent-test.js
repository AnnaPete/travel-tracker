import chai from 'chai';
const expect = chai.expect;

describe('Agent', function() {
  this.beforeEach();

  it.skip('should see a list of travelers', function() {
    expect().to.equal();
  });

  it.skip('should see a list of trips', function() {
    expect().to.equal();
  });

  it.skip('should see a list of destinations', function() {
    expect().to.equal();
  });

  it.skip('should have a method that calculates the total income for the year', function() {
    expect().to.equal();
  });

  it.skip('should have a method that finds trips by year', function() {
    expect().to.equal();
  });

  it.skip('should have a method that finds travelers by name', function() {
    expect().to.equal();
  });
});
